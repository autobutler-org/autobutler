package views

import (
"github.com/exokomodo/exoflow/autobutler/backend/ui/components"
)

templ Index() {
    <!DOCTYPE html>
    <html lang="en">
        @components.Header()
        <body class="min-h-screen w-full">
            <main>
            </main>
        </body>
        @components.Footer()
    </html>
}
