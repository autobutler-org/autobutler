package views

import (
"github.com/exokomodo/exoflow/autobutler/backend/ui/components"
"github.com/exokomodo/exoflow/autobutler/backend/ui/components/hero"
)

templ Home() {
    <!DOCTYPE html>
    <html lang="en">
        @components.Header()
        @components.Body() {
            @hero.Hero()
        }
        @components.Footer()
    </html>
}
