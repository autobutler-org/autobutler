apiVersion: v1
kind: Service
metadata:
  name: {{ include "autobutler-router.fullname" . }}
  labels:
    {{- include "autobutler-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autobutler-router.selectorLabels" . | nindent 4 }}
