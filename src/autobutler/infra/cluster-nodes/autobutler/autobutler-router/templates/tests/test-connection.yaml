apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autobutler-router.fullname" . }}-test-connection"
  labels:
    {{- include "autobutler-router.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "autobutler-router.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
