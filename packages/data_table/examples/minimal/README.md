# data_table_example_minimal

Run this example from the `packages/data_table/examples/minimal` directory:

```bash
cd packages/data_table/examples/minimal
flutter pub get
flutter run
```

This small app demonstrates using `processTable` to compute sorted/filtered
rows and rendering them with native Flutter widgets.
