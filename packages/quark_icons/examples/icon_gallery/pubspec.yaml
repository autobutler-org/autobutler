name: quark_icons_example_icon_gallery
description: Browseable icon gallery for the quark_icons package.
publish_to: "none"

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  quark_icons:
    path: ../..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
