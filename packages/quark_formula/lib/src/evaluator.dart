// Minimal evaluator placeholder for quark_formula package

class Evaluator {
  /// Evaluate a simple AST produced by Parser
  dynamic evaluate(dynamic ast) {
    // placeholder: just return the AST
    return ast;
  }
}
