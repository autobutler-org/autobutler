name: quark_formula
description: Formula engine extracted from data_table
version: 0.0.1
homepage: "https://github.com/autobutler-org/quark"
license: "MIT"
environment:
  sdk: ">=3.0.0 <4.0.0"

# Pure Dart package (no Flutter)

dependencies:
  # add runtime deps here if needed

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
