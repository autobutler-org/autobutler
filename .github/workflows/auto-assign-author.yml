name: Auto-Assign Author

on:
  pull_request_target:
    types:
      - opened
      - reopened

jobs:
  auto-assign-author:
    runs-on: ubuntu-latest
    steps:
      - uses: autobutler-org/auto-assign-author@main
