name: Performance Tests

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: "0 7 * * *"

permissions:
  contents: read

defaults:
  run:
    shell: bash

env:
  AUTOBUTLER_USERNAME: perf
  AUTOBUTLER_PASSWORD: perf-password

jobs:
  autobutler-performance-loadtest:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Setup sqlc
        uses: ./.github/actions/setup-sqlc

      - name: Setup swag
        uses: ./.github/actions/setup-swag

      - name: Setup wrk
        uses: ./.github/actions/setup-wrk

      - name: Run load test profile
        run: |
          make test/perf/load

      - name: Upload load test artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: loadtest-results
          path: test-results/performance/
          if-no-files-found: warn

  autobutler-performance-stress:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Setup sqlc
        uses: ./.github/actions/setup-sqlc

      - name: Setup swag
        uses: ./.github/actions/setup-swag

      - name: Setup wrk
        uses: ./.github/actions/setup-wrk

      - name: Run stress profile
        run: |
          make test/perf/stress

      - name: Upload stress test artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: stress-results
          path: test-results/performance/
          if-no-files-found: warn

  autobutler-performance-summary:
    runs-on: ubuntu-latest
    needs:
      - autobutler-performance-loadtest
      - autobutler-performance-stress
    if: always()
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Download load test results
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: loadtest-results
          path: test-results/performance-summary/load

      - name: Download stress test results
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: stress-results
          path: test-results/performance-summary/stress

      - name: Render performance summary
        run: |
          tree test-results
          make test/perf/summary >> ${GITHUB_STEP_SUMMARY}
