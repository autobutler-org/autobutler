name: Setup Node
runs:
  using: composite
  steps:
    - name: Setup Node
      uses: actions/setup-node@v7
      with:
        node-version-file: .nvmrc
    - name: Install node modules
      shell: bash
      run: |
        npm ci
